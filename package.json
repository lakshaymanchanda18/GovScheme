{
  "name": "govscheme-monorepo",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "prisma generate",
    "build": "npx prisma generate && cd server && npm run build && cd ../client && npm run build"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
