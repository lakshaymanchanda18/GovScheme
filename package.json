{
  "name": "govscheme-monorepo",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "prisma generate",
    "build": "npx prisma@5.22.0 generate --schema=./prisma/schema.prisma && npx esbuild server/src/index.ts --bundle --platform=node --target=node18 --outfile=api/index.js --external:@prisma/client --external:prisma && cd client && vite build"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
